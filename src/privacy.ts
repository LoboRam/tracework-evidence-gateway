const forbiddenPatterns: ReadonlyArray<readonly [RegExp, string]> = [
  [/(?:sk|rk|pk)-(?:live|test|proj)?[_-]?[A-Za-z0-9_-]{16,}/i, "possible_api_credential"], [/(?:ghp|github_pat|xox[baprs]|AKIA)[A-Za-z0-9_-]{12,}/i, "possible_service_credential"], [/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/i, "private_key"], [/\bBearer\s+[A-Za-z0-9._~+/=-]{16,}/i, "bearer_token"], [/\beyJ[A-Za-z0-9_-]{10,}\.eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{8,}\b/, "json_web_token"], [/(?:api[_ -]?key|password|secret|access[_ -]?token)\s*[:=]\s*["']?[A-Za-z0-9_./+\-=]{8,}/i, "credential_assignment"], [/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/i, "email_address"], [/\b(?:\d[ -]*?){13,19}\b/, "possible_payment_card"], [/\b\d{3}-\d{2}-\d{4}\b/, "possible_government_identifier"], [/(?:https?:\/\/)(?:localhost|127\.0\.0\.1|10\.|192\.168\.|172\.(?:1[6-9]|2\d|3[01])|[^/\s]+\.(?:internal|local))[^\s]*/i, "private_url"], [/```[\s\S]{80,}```/, "source_code_block"], [/\b(?:User|Assistant|Human|Claude|ChatGPT|System):\s.{20,}[\r\n]+(?:User|Assistant|Human|Claude|ChatGPT|System):/i, "raw_transcript"], [/(?:^|\n)(?:prompt|response|completion)\s*:\s*.{80,}(?:\n|$)/i, "raw_prompt_dump"], [/\[\s*\{[^{}\n]{0,120}["']role["']\s*:\s*["'](?:user|assistant|system)["'][\s\S]{0,240}["']content["']\s*:/i, "serialized_message_array"], [/\bdiff --git\s+a\/[^\s]+\s+b\/[^\s]+[\s\S]{40,}(?:@@|index\s+[a-f0-9]+)/i, "repository_diff"], [/(?:^|\n)(?:FILE|PATH):\s*[^\n]+\n[\s\S]{160,}(?:^|\n)(?:FILE|PATH):\s*[^\n]+/im, "bulk_file_payload"],
];
export function collectProtocolStrings(value: unknown, path = "payload", output: Array<{ path: string; value: string }> = []) { if (typeof value === "string") output.push({ path, value }); else if (Array.isArray(value)) value.forEach((item, index) => collectProtocolStrings(item, `${path}[${index}]`, output)); else if (value && typeof value === "object") Object.entries(value).forEach(([key, item]) => collectProtocolStrings(item, `${path}.${key}`, output)); return output; }
export function privacyIssuesFor(value: unknown, root = "payload"): string[] { const issues: string[] = []; for (const item of collectProtocolStrings(value, root)) { if (item.value.length > 180 && /^[A-Za-z0-9+/=_-]+$/.test(item.value) && item.value.length % 4 === 0) issues.push(`${item.path}: suspicious_encoded_blob`); if (item.value.split("\n").some((line) => line.length > 420)) issues.push(`${item.path}: source_sized_line`); const codeSignals = (item.value.match(/[{};]|=>|\b(?:const|let|function|class|import|SELECT|CREATE TABLE)\b/g) ?? []).length; if (item.value.length > 350 && codeSignals >= 8) issues.push(`${item.path}: likely_source_code`); for (const [pattern, category] of forbiddenPatterns) if (pattern.test(item.value)) issues.push(`${item.path}: ${category}`); } return [...new Set(issues)]; }
